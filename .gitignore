# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# OS
Thumbs.db

# Test coverage
coverage

# Playwright E2E artifacts
test-results/
playwright-report/
blob-report/
playwright/.cache/

# TypeScript build info
*.tsbuildinfo

# Vite config compiled artifacts (only keep .ts source)
vite.config.js
vite.config.d.ts

# Internal dev spec (not for open-source release)
todo-calendar-dev-spec.html

# Blog posts / promotional drafts (not part of the project source)
blog/

# Internal dev notes (version goals & outcomes, only for dev process)
dev-notes/

# Local workspace memory (agent session notes, not for open-source release)
.workbuddy/
